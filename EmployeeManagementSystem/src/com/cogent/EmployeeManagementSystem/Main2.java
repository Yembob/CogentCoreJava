package com.cogent.EmployeeManagementSystem;

public class Main2 {
	public static void main(String[] args) {

		/*
		 * try { String ip = "192.168.1.1";
		 * 
		 * String [] ipAddress = ip.split("\\."); for (String string : ipAddress){ int a
		 * = Integer.parseInt(string); if( a>=0 && a<=255) {
		 * 
		 * } else System.exit(0);
		 * 
		 * } int classCell = Integer.parseInt(ipAddress[0]); if(classCell>0 &&
		 * classCell<=126) { System.out.println("IP belongs to class A");
		 * 
		 * } else (classCell>0 && classCell<=126) else (classCell>0 && classCell<=126)
		 * else (classCell>0 && classCell<=126) } catch (NumberFormatException e) { //
		 * TODO Auto-generated catch block e.printStackTrace();
		 */
		// substring
		// String s = "Anthony Yemets";

		// String result = s.substring(0,7);
		// System.out.println(result);

		// System.out.println("A".codePointAt(0));
		// System.out.println("Ant".codePointAt(0));
		// the compare to method uses the difference on the ASCII
		// System.out.println("Ant".compareTo("aDI"));
		
		
	// Buffers Lesson	
		
	/*
	 * StringBuffer buffer = new StringBuffer("Anthony");
	 * buffer.append("Abhinandan"); System.out.println(buffer.capacity());
	 * System.out.println(buffer.length()); buffer.append("Anthony");
	 * 
	 * buffer.append("Abhinandan"); System.out.println(buffer.capacity());
	 * System.out.println(buffer.length()); buffer.append("Anthony");
	 * 
	 * 
	 * buffer.append("Abhinandan"); System.out.println(buffer.capacity());
	 * System.out.println(buffer.length()); buffer.append("Anthony");
	 * 
	 * 
	 * 
	 * 
	 * buffer.append("Abhinandan"); System.out.println(buffer.capacity());
	 * System.out.println(buffer.length()); buffer.append("Anthony");
	 */
		
		//this method deletes
		/*
		 * StringBuffer buffer = new
		 * StringBuffer("Anthony is the best human being around"); StringBuffer Ant =
		 * new StringBuffer("222"); buffer.delete(0, 5); buffer.compareTo(buffer);
		 * 
		 * System.out.println( buffer.compareTo(Ant));
		 */
		
		
	}

}
